Use case: precise-object-edit / technical animation restoration.

Input image 1 is the EDIT TARGET: an existing square 384 by 384 pixel crop of one intermediate animation frame. Images 2 and 3 are identity and line-quality REFERENCES only, from the two surrounding source frames. Edit image 1 only. Do not substitute either reference's pose or proportions.

Perform an extremely localized restoration on image 1. Remove the faint doubled, translucent echo contours immediately beside the main dark outlines around the orange-haired woman's head and hands, and around the sloth's head and hands. Trace one clean, crisp primary contour in the exact location of the existing strongest contour. Erase only the faint duplicate echo and restore the immediately adjacent original flat fill/background. Preserve the existing cartoon line weight, natural antialiasing, and original slightly textured video appearance. This is not a redraw, enhancement, redesign, or style change.

Strict invariants: keep the exact image-1 pose, silhouette position, anatomy, hand position and visible fingers, character sizes, hair shape, tracksuit seams, sloth fur details, colors, lighting, and framing. Keep the library columns, ceiling, doorway, stairs, shadows, every background detail, and the complete crop boundary unchanged. No camera shift, crop, zoom, or reframing. Do not add or remove characters or objects. Keep all pixels outside the narrow bands immediately surrounding the specified head/hand contours unchanged. Do not sharpen, repaint, denoise, smooth, recolor, or regenerate the whole image. The already-clean contours and all clothing/legs/feet must stay unchanged. Reference images supply only examples of clean original linework and identity; target image 1 determines everything spatial and temporal.

Return only the edited square target image, with exactly the same composition and canvas as image 1. No labels, annotations, collage, or comparison layout. No invented intermediate pose.
