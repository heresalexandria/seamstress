# Current quality status

No full render has met the user's imperceptible-seam requirement.

Latest user review: `IYTYT-source-conform-eight-joins.mp4` is much better and mostly seamless, but local color changes remain visible at the joins, including the first join three seconds into the representative preview. The user requested a Git checkpoint and clear reproduction instructions before any further color treatment. This checkpoint contains the existing framing and protected global grading only; no new local-color model has been implemented.

The user explicitly rejected the RIFE full movie (`output/IYTYT-bridged.mp4`): visible morphing at every join was more distracting than the original hard cuts. Do not present it as the recommended or completed repair. Earlier dense-field renders were also rejected. Numerical seam-error reductions did not predict acceptable viewing quality.

Earlier short experiment: `output/source-proof/first-join-candidate.mp4`. It covers source frames 289–432, with the first cut three seconds into the six-second excerpt. It uses one original drawing per output frame, a fixed affine correction on the incoming clip, a 24-frame camera-rate adjustment, and constant incoming RGB grading. No neural interpolation, crossfade, synthesized pose, or dense image deformation is used. It retains the original frame order and cadence. The view has a 0.556% total safety crop; incoming framing correction persists rather than reverting immediately. This one-sided recipe was not used for the full baseline; its representative preview is `output/source-proof/current-plan-first-join.mp4` instead.

Landmark checks established that the first continuation is compressed vertically relative to horizontally. A fixed affine correction aligns the windshield/car/face considerably better than a similarity transform. Some reflection and background details still differ. These differences must be judged in motion; this test is not certified seamless.

Completed geometry research measured fixed affine corrections at the remaining joins and rejected cumulative framing drift. Separate grading research found that naive constant propagation through all clips collapses contrast. The full baseline therefore uses slow, symmetric, bounded tone curves.

`seamstress conform` is a new source-frame renderer under development. It supports saved fixed segment transforms and optional per-frame global transforms, checks the source fingerprint, protects existing files, and preserves frame indices. The full-length eight-join experiment has rendered to `output/IYTYT-source-conform-eight-joins.mp4`, reproduced by `plans/IYTYT-eight-joins.json`. It includes nine protected color curves, eight global geometry corrections, and a verified 5.924% total viewing crop with no exposed source edges. Frame 2888 deliberately has no geometric correction and remains unresolved. It is not a completed or accepted repair.

The source `IYTYT.mp4` is unchanged. The `.env` file has not been read. One built-in image-generation call made a static clean city background for disoccluded regions at 2888. Native recompositions at 2888 and 2896 preserve source foreground interiors, remove duplicate flame strips, and fill actor-zone holes. A background homography worsened alignment and was rejected. The subsequent two-second layer proof was also rejected: native frames show duplicate pale contours beneath the woman's extended right arm/hand and around both characters' feet, plus city/sky halos. Affine mask transport does not preserve all articulated boundaries, and a single plate transform drifts across scene depths. A short grading return also confounds that proof, but the visible contour failure is independent of grading. No generated patch is in the full-length conform movie. Further layer work would need accurate per-frame mattes and depth-aware background reconstruction; another whole-image morph is not justified by this evidence.

An independent exact-drawing retiming proof at 2527 confirms an eight-frame startup hold. Omitting six redundant source pictures reduces it to the ordinary two-frame exposure, but preserving duration requires six three-frame exposures during recovery and advances action pictures by up to 250.25 ms against the existing audio. This is not an unambiguous improvement and has not been integrated. Evidence and the separate proof are in `research/retiming2527/`.

The reusable calibrated pipeline is implemented: `seamstress design-conform` reconstructs the current plan from `plans/IYTYT-calibration.json`, and `seamstress conform` renders it. All 3,347 rebuilt transforms agree within 1.6e-15, with identical viewing crop and grading LUTs. An actual rebuilt-plan preview has the same decoded-video SHA-256 as the earlier-plan preview; evidence is in `output/source-proof/reproduction-check.json`. The representative six-second excerpt of the full eight-join plan is `output/source-proof/current-plan-first-join.mp4`, with the cut three seconds in. This differs from the earlier one-sided first proof. The full test suite now passes 78 tests. These are engineering checks, not perceptual acceptance.
